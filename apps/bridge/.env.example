# Untron Bridge UI (public env)
# Copy to `.env` for local dev.

# Bridge API base URL.
# - Production: https://api.untron.finance/bridge
# - Local dev (per openapi.json): http://localhost:8080
PUBLIC_UNTRON_BRIDGE_API_URL=http://localhost:8080

# Cloudflare Turnstile site key used to obtain the `cf-turnstile-response` token
# required by `POST /v1/orders`.
PUBLIC_TURNSTILE_SITE_KEY=
