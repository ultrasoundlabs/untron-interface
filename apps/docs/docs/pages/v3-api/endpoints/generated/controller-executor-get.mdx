---
title: "GET /controller_executor"
description: "Current controller executor (singleton)"
---

import { EndpointLayout } from '../../../../components/EndpointLayout'
import { EndpointAside } from '../../../../components/EndpointAside'

<EndpointLayout
  title="Current controller executor (singleton)"
  method="GET"
  path="/controller_executor"
  summary="Current controller executor (singleton)"
  aside={
    <EndpointAside
      baseUrl="https://api.untron.finance/v3"
      path="/controller_executor"
      method="get"
      specUrl="https://api.untron.finance/v3/openapi.json"
    />
  }
>
  :::info[Category]
  PostgREST view.
  :::

  ## Request

:::code-group

```bash [cURL]
curl -sS 'https://api.untron.finance/v3/controller_executor'
```

:::

  ## Response

```json
[
  {
    "executor": "…",
    "valid_from_seq": 0,
    "valid_to_seq": 0
  }
]
```
</EndpointLayout>
