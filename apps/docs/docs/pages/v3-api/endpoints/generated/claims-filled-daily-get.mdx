---
title: "GET /claims_filled_daily"
description: "Daily rollup of filled claims (current rows only; timestamp from ClaimFilled event)."
---

import { EndpointLayout } from '../../../../components/EndpointLayout'
import { EndpointAside } from '../../../../components/EndpointAside'

<EndpointLayout
  title="Daily rollup of filled claims (current rows only; timestamp from ClaimFilled event)."
  method="GET"
  path="/claims_filled_daily"
  summary="Daily rollup of filled claims (current rows only; timestamp from ClaimFilled event)."
  aside={
    <EndpointAside
      baseUrl="https://api.untron.finance/v3"
      path="/claims_filled_daily"
      method="get"
      specUrl="https://api.untron.finance/v3/openapi.json"
    />
  }
>
  :::info[Category]
  PostgREST view.
  :::

  ## Request

:::code-group

```bash [cURL]
curl -sS 'https://api.untron.finance/v3/claims_filled_daily'
```

:::

  ## Response

```json
[
  {
    "amount_usdt_total": "…",
    "claims_filled_total": 0,
    "day": "…"
  }
]
```
</EndpointLayout>
