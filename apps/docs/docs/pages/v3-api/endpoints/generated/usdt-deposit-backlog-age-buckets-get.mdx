---
title: "GET /usdt_deposit_backlog_age_buckets"
description: "Deposit backlog grouped into age buckets (relative to now())."
---

import { EndpointLayout } from '../../../../components/EndpointLayout'
import { EndpointAside } from '../../../../components/EndpointAside'

<EndpointLayout
  title="Deposit backlog grouped into age buckets (relative to now())."
  method="GET"
  path="/usdt_deposit_backlog_age_buckets"
  summary="Deposit backlog grouped into age buckets (relative to now())."
  aside={
    <EndpointAside
      baseUrl="https://api.untron.finance/v3"
      path="/usdt_deposit_backlog_age_buckets"
      method="get"
      specUrl="https://api.untron.finance/v3/openapi.json"
    />
  }
>
  :::info[Category]
  PostgREST view.
  :::

  ## Request

:::code-group

```bash [cURL]
curl -sS 'https://api.untron.finance/v3/usdt_deposit_backlog_age_buckets'
```

:::

  ## Response

```json
[
  {
    "age_bucket": "…",
    "amount_total": "…",
    "deposits_total": 0,
    "recommended_action": "…"
  }
]
```
</EndpointLayout>
