---
title: "GET /hub_lp_balances"
description: "Current derived LP principal balances (KV)"
---

import { EndpointLayout } from '../../../../components/EndpointLayout'
import { EndpointAside } from '../../../../components/EndpointAside'

<EndpointLayout
  title="Current derived LP principal balances (KV)"
  method="GET"
  path="/hub_lp_balances"
  summary="Current derived LP principal balances (KV)"
  aside={
    <EndpointAside
      baseUrl="https://api.untron.finance/v3"
      path="/hub_lp_balances"
      method="get"
      specUrl="https://api.untron.finance/v3/openapi.json"
    />
  }
>
  :::info[Category]
  PostgREST view.
  :::

  ## Request

:::code-group

```bash [cURL]
curl -sS 'https://api.untron.finance/v3/hub_lp_balances'
```

:::

  ## Response

```json
[
  {
    "balance": "…",
    "lp": "…",
    "valid_from_seq": 0,
    "valid_to_seq": 0
  }
]
```
</EndpointLayout>
