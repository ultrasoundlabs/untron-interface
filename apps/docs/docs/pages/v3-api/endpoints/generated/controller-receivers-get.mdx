---
title: "GET /controller_receivers"
description: "Current mapping of receiver salts to deployed receiver addresses (KV)"
---

import { EndpointLayout } from '../../../../components/EndpointLayout'
import { EndpointAside } from '../../../../components/EndpointAside'

<EndpointLayout
  title="Current mapping of receiver salts to deployed receiver addresses (KV)"
  method="GET"
  path="/controller_receivers"
  summary="Current mapping of receiver salts to deployed receiver addresses (KV)"
  aside={
    <EndpointAside
      baseUrl="https://api.untron.finance/v3"
      path="/controller_receivers"
      method="get"
      specUrl="https://api.untron.finance/v3/openapi.json"
    />
  }
>
  :::info[Category]
  PostgREST view.
  :::

  ## Request

:::code-group

```bash [cURL]
curl -sS 'https://api.untron.finance/v3/controller_receivers'
```

:::

  ## Response

```json
[
  {
    "receiver": "…",
    "receiver_salt": "…",
    "valid_from_seq": 0,
    "valid_to_seq": 0
  }
]
```
</EndpointLayout>
