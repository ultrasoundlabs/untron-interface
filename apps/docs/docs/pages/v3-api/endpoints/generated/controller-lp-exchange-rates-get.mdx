---
title: "GET /controller_lp_exchange_rates"
description: "Current LP exchange rates for non-USDT tokens (KV)"
---

import { EndpointLayout } from '../../../../components/EndpointLayout'
import { EndpointAside } from '../../../../components/EndpointAside'

<EndpointLayout
  title="Current LP exchange rates for non-USDT tokens (KV)"
  method="GET"
  path="/controller_lp_exchange_rates"
  summary="Current LP exchange rates for non-USDT tokens (KV)"
  aside={
    <EndpointAside
      baseUrl="https://api.untron.finance/v3"
      path="/controller_lp_exchange_rates"
      method="get"
      specUrl="https://api.untron.finance/v3/openapi.json"
    />
  }
>
  :::info[Category]
  PostgREST view.
  :::

  ## Request

:::code-group

```bash [cURL]
curl -sS 'https://api.untron.finance/v3/controller_lp_exchange_rates'
```

:::

  ## Response

```json
[
  {
    "exchange_rate": "…",
    "token": "…",
    "valid_from_seq": 0,
    "valid_to_seq": 0
  }
]
```
</EndpointLayout>
