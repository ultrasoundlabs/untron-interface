---
title: "GET /usdt_deposit_funnel_daily"
description: "Deposit funnel time-series. `stage` is `recommended_action` plus an `all` row per day."
---

import { EndpointLayout } from '../../../../components/EndpointLayout'
import { EndpointAside } from '../../../../components/EndpointAside'

<EndpointLayout
  title="Deposit funnel time-series. `stage` is `recommended_action` plus an `all` row per day."
  method="GET"
  path="/usdt_deposit_funnel_daily"
  summary="Deposit funnel time-series. `stage` is `recommended_action` plus an `all` row per day."
  aside={
    <EndpointAside
      baseUrl="https://api.untron.finance/v3"
      path="/usdt_deposit_funnel_daily"
      method="get"
      specUrl="https://api.untron.finance/v3/openapi.json"
    />
  }
>
  :::info[Category]
  PostgREST view.
  :::

  ## Request

:::code-group

```bash [cURL]
curl -sS 'https://api.untron.finance/v3/usdt_deposit_funnel_daily'
```

:::

  ## Response

```json
[
  {
    "amount_total": "…",
    "day": "…",
    "deposits_total": 0,
    "stage": "…"
  }
]
```
</EndpointLayout>
