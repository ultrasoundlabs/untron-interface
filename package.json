{
  "name": "untron-interface",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "cd apps/bridge && bun dev",
    "build": "cd apps/bridge && bun run build",
    "preview": "cd apps/bridge && bun run preview",
    "lint": "cd apps/bridge && bun run lint",
    "check": "cd apps/bridge && bun run check",
    "test": "cd apps/bridge && bun run test"
  }
}
